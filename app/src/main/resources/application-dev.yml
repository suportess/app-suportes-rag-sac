spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: false

logging:
  level:
    com.company.specvalidator: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
